import { DirectiveDropFileDirective } from './directive-drop-file.directive';

describe('DirectiveDropFileDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveDropFileDirective();
    expect(directive).toBeTruthy();
  });
});

